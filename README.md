# boilerplate
State machine and sensor handler boilerplate code/libraries.

## Submodules

- **canard_lqr_controller** – LQR gain-scheduled canard controller. Use with rockets that have canard actuation; gain table comes from MATLAB `solveLQR.m`. See `canard_lqr_controller/README.md` for usage.
